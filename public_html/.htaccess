php_value memory_limit 512M
<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On


    # Перенаправление всех запросов на index.php (кроме существующих файлов/папок)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]




</IfModule>

# Включение сжатия Gzip (опционально, улучшает скорость)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Кэширование статики (CSS, JS, изображения)
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>

# AuthType Basic
#AuthName "Restricted Area"
# AuthUserFile /home/f/fr7905kx/checker/public_html/.htpasswd
#Require valid-user